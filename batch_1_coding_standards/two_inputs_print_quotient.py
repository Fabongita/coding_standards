# define a function for quotient
def quotient(first_number,second_number):
    return first_number/second_number
# set up the input variables
first_number = int(input("input the first number "))
second_number = int(input("input the second number "))
# print the quotient
print("the quotient is", quotient(first_number,second_number))